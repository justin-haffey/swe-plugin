interface:
  display_name: "Local RAG Index Folder"
  short_description: "Register and index one local folder"
  default_prompt: "Use $rag-index to register and index the local folder I specify."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "local_rag_mgmt"
      description: "Plugin-provided privileged Local RAG management MCP server"
