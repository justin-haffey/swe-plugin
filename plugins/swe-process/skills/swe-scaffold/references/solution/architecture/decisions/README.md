# Solution Decisions

Store Solution ADRs as `ADR-###-short-name.md`. Package ADRs belong under the owning Package's `decisions/` directory. ADR numbering is local to each decisions directory and IDs remain stable after acceptance.
