interface:
  display_name: "Bridge"
  short_description: "Track a request in its exact child repository"
  default_prompt: "Use $bridge to prepare this request for the named repository and track its result through a supported authorized transport."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
