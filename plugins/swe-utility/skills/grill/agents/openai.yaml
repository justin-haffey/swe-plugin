interface:
  display_name: "Grill"
  short_description: "Pressure-test plans through focused questions"
  default_prompt: "Use $grill to pressure-test the current plan, decision, or idea."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
