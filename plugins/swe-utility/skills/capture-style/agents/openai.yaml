interface:
  display_name: "Capture Style"
  short_description: "Extract a reusable writing style card"
  default_prompt: "Use $capture-style to extract a STYLE_CARD from the provided writing samples."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
