interface:
  display_name: "Prototype Mode"
  short_description: "Toggle scoped prototype-first delivery and backtracking"
  default_prompt: "Use $prototype -on to enable scoped prototype-first delivery, or $prototype -off to reconcile and disable it."

policy:
  allow_implicit_invocation: false
