interface:
  display_name: "Diagram Design"
  short_description: "Use when prompted. Generates self-contained HTML files with inline SVG and CSS."
  default_prompt: "use $diagram-design to create the diagram(s)"

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
