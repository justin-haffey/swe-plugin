interface:
  display_name: "Mimic Style"
  short_description: "Apply a style card to Codex instructions"
  default_prompt: "Use $mimic-style to apply the provided STYLE_CARD to a narrowly scoped Codex agent."

policy:
  allow_implicit_invocation: false

dependencies:
  tools: []
