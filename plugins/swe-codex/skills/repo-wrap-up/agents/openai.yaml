interface:
  display_name: "Repository Wrap-Up"
  short_description: "Reconcile docs and prepare user review"
  default_prompt: "Use $repo-wrap-up to reconcile repository guidance, run repository-native checks, and pause with an exact user-review handoff."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
